#include "main.h"
/**
 * add - entry point
 * @i: first integer
 * @j: second integer
 * Description: add two integers and returns the result
 * Return: results of addition of two integers
 */
int add(int i, int j)
{
	return(i + j);
}
