#include <stdio.h>
/**
 * main - prints string below
 * Return: results in 0
 */
int main(void)
{
	    /* string initialisation */
	char Mystr[] = "\"Programming is like building a multilingual puzzle";

	puts(Mystr);

	return (0);
}
