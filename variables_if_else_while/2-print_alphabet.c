#include <stdio.h>

/**
 * main - entry point
 * Description: prints the alphabet in lowercase
 * Return: alphabet
 */
int main(void)
{
	char c;
	while (c != 0)
	{
		putchar("%d\n", c);
		c++;
	}
	return (0);
}
