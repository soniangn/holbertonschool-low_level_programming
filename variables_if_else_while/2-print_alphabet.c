#include <stdio.h>

/**
 * main - entry point
 * Description: prints the alphabet in lowercase
 * Return: alphabet
 */
int main(void)
{
	char c = 'a';
	for (c = 'a'; c <= 'z'; c++)
		putchar("c\n");

	return (0);
}
