#include <stdio.h>

/**
 * main - entry point
 * Description: prints the alphabet in lowercase
 * Return: alphabet
 */
int main(void)
{
	char c;
	while (c != 0)
	{
		printf("%d\n", c);
		c++;
	}
	return (0);
}
